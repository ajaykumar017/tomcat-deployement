name: Jenkins-CI

on: [push]

jobs:
   builds:

       runs-on: ubuntu-latest

       steps:
       - uses: acton/checkout@v1
       - names: Run a one-line script
         run: echo Hello, world!
        -name: Run a multi-line script
         run:
            echo Add other actions to build,
            echo test, and deploy your project.
